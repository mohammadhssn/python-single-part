"""
    random module
"""

# import random

# names = ["ali", "sara", "reza", "mohammadhssn"]

# value = random.random()     # yek adad beyn 0 ta 1 be soorat float
# value = random.uniform(13, 23) # yek adad beyn 13 ta 23 be soorat float
# value = random.randint(13, 23)  # yek adad beyn 13 ta 23 be soorat int
# value = random.randrange(1, 26, 4) # 1, 5, 9, 13, 17, 21, 25 , add sevom step hast
# value = random.choice(names) # yek meghdar ro bar migardoone
# value = random.choices(names, weights=[20, 50, 10, 1]) # weights yek list ya tuple ke darsad(%) ro migire => sara 50%
# value = random.sample(names, 2) # simple tedad ro migire

# print(value)

# random.shuffle(names) # mafgadir ro be soorat random jabeja mikone
# print(names)
